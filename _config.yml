
gems: [jekyll-paginate]
# Permalinks
#
# Use of `relative_permalinks` ensures post links from the index work properly.
permalink:           pretty
markdown: rdiscount

# Setup
title:               Metiix Blog
tagline:             Managed Backup Services
siteurl:                 http://blog.metiix.com
paginate:	5
baseurl:             ""

# Assets
#
# We specify the directory for Jekyll so we can use @imports.
sass:
  sass_dir:          _sass
  style:            :compressed


prose:
  rooturl: '_posts'
  siteurl: 'http://blog.metiix.com/'
  relativeLinks: 'http://blog.metiix.com/links.jsonp'
  media: 'media'
  ignore:
    - index.md
    - _config.yml
    - /_layouts
    - /_includes
  metadata:
    _posts:
      - name: "category"
        field:
          element: "hidden"
          value: "blog"
      - name: "layout"
        field:
          element: "hidden"
          value: "post"
      - name: "title"
        field:
          element: "text"
          value: ""
      - name: "tags"
        field:
          element: "multiselect"
          label: "Add Tags"
          placeholder: "Choose Tags"
          alterable: true- name: "splash"
        field:
          element: "text"
          label: "Splash image"
          value: ""
